{
    "name": "change_log",
    "description": "Append an entry to the system changelog located in /var/log/ash.log.  Please use this function after you make any changes to the system.",
    "parameters": {
        "type": "object",
        "properties": {
            "changes": {
                "type": "string",
                "description": "Describe the changes you made to the system.  A timestamp will be appended to the beginning of the string, you do not need to provide it. Use markdown formatting if helpful."
            }
        },
        "required": [
            "changes"
        ]
    }
}