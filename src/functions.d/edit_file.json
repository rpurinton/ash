{
    "name": "edit_file",
    "description": "Performs in-place editing of a file on the filesystem using a provided sed command. To test your changes first perform a dry run using the -n option with sed.",
    "parameters": {
        "type": "object",
        "properties": {
            "shell_command": {
                "description": "The full properly escaped shell command line to edit the file using ie: \"sed -i 's/old/new/g' /path/to/file\"",
                "type": "string"
            }
        },
        "required": [
            "shell_command"
        ]
    }
}