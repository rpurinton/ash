{
    "name": "edit_file",
    "description": "Edit a file on the local filesystem using a sed command",
    "parameters": {
        "type": "object",
        "properties": {
            "sed_command": {
                "type": "string",
                "description": "The complete sed command to be run to edit the file in the desired manner: e.g. \"sed -i 's/old/new/g' /path/to/file\" you can use any options available in sed."
            }
        },
        "required": [
            "sed_command"
        ]
    }
}