{
    "name": "edit_file",
    "description": "Performs in-place editing of a file on the filesystem using a provided sed command. It can simulate changes with a dry run.",
    "parameters": {
        "type": "object",
        "properties": {
            "sed_command": {
                "description": "The sed command line to edit the file, ex: \"sed -i 's/old/new/g' /path/to/file\"",
                "type": "string"
            }
        },
        "required": [
            "sed_command"
        ]
    }
}